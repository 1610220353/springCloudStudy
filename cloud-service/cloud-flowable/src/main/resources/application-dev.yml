#服务器端口
server:
  port: 8101
#数据源配置
spring:
  datasource:
    url: ${blade.datasource.fims-data.url}
    username: ${blade.datasource.fims-data.username}
    password: ${blade.datasource.fims-data.password}

